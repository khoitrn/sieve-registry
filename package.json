{
  "name": "sieve-registry",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute sieve-registry --local --file=migrations/0001_init.sql && wrangler d1 execute sieve-registry --local --file=migrations/0002_sources.sql && wrangler d1 execute sieve-registry --local --file=migrations/0003_pin_sha.sql && wrangler d1 execute sieve-registry --local --file=migrations/0004_bundles.sql && wrangler d1 execute sieve-registry --local --file=migrations/0005_pin_and_flag.sql && wrangler d1 execute sieve-registry --local --file=migrations/0006_add_mattpocock_source.sql",
    "db:migrate:remote": "wrangler d1 execute sieve-registry --remote --file=migrations/0001_init.sql && wrangler d1 execute sieve-registry --remote --file=migrations/0002_sources.sql && wrangler d1 execute sieve-registry --remote --file=migrations/0003_pin_sha.sql && wrangler d1 execute sieve-registry --remote --file=migrations/0004_bundles.sql && wrangler d1 execute sieve-registry --remote --file=migrations/0005_pin_and_flag.sql && wrangler d1 execute sieve-registry --remote --file=migrations/0006_add_mattpocock_source.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260716.1",
    "typescript": "^5.7.0",
    "wrangler": "^4.114.0"
  }
}
